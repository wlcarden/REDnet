# Secrets (NEVER commit)
deploy/rednet.env
deploy/.env
deploy/mas/config.yaml
deploy/draupnir/config/
deploy/ansible/inventory.ini
deploy/ansible/group_vars/all.yml
*.key
*.pub
*.token

# Governance data (vouch provenance, bot tokens, invite cards)
deploy/vouch.jsonl
deploy/gov-bot/.env
deploy/invites/

# Config rendered from templates (contains secrets)
deploy/element-web/config.json

# Deploy runtime (credentials + state markers)
deploy/.first-run-credentials
deploy/.deploy-messages-posted

# Backups and restic repos
deploy/backups/
deploy/restic-repo/

# Build artifacts
node_modules/
lib/
dist/
*.js.map

# Lock files (regenerated per environment)
package-lock.json

# Prototype secrets
prototype/onboarding-mas/mas/config.yaml

# Docker / runtime
*.log
synapse_data/
postgres_data/
mas_data/
grafana_data/

# Claude Code
.claude/

# OS
.DS_Store
Thumbs.db
