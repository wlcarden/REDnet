# Auto-provision Prometheus as Grafana's default datasource. Import a Synapse dashboard
# (Grafana.com dashboard ID 10046, the official matrix.org Synapse board) once running.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
