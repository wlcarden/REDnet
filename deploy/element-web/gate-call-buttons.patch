diff --git a/src/components/views/rooms/RoomHeader.tsx b/src/components/views/rooms/RoomHeader.tsx
index c2642ea..9a0f948 100644
--- a/src/components/views/rooms/RoomHeader.tsx
+++ b/src/components/views/rooms/RoomHeader.tsx
@@ -331,14 +331,19 @@ export default function RoomHeader({
 
                     {isViewingCall && <CallGuestLinkButton room={room} />}
 
-                    {hasActiveCallSession && !isConnectedToCall && !isViewingCall ? (
-                        joinCallButton
-                    ) : (
-                        <>
-                            {!isVideoRoom && videoCallButton}
-                            {!useElementCallExclusively && !isVideoRoom && voiceCallButton}
-                        </>
-                    )}
+                    {/* REDnet: only show call UI when the calls profile is enabled
+                        (feature_group_calls). The legacy 1:1 voice/video buttons render
+                        regardless otherwise, but REDnet has no legacy VoIP path (P2P off,
+                        no TURN), so they would only dead-end. */}
+                    {groupCallsEnabled &&
+                        (hasActiveCallSession && !isConnectedToCall && !isViewingCall ? (
+                            joinCallButton
+                        ) : (
+                            <>
+                                {!isVideoRoom && videoCallButton}
+                                {!useElementCallExclusively && !isVideoRoom && voiceCallButton}
+                            </>
+                        ))}
 
                     <Tooltip label={_t("right_panel|room_summary_card|title")}>
                         <IconButton
